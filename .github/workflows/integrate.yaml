# Build (and cache?)
# Integration Test

name: Integrate

on:
  workflow_call:

jobs:
  setup:
    name: Setup
    runs-on: ubuntu-latest
    steps:
      - run: echo "SETUP"
  
  # build:
  #   # name: Build
  #   uses: ./.github/workflows/build.yaml

  # test:
  #   # name: Test
  #   uses: ./.github/workflows/test.yaml
